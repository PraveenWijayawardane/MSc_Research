Hospital profile uploads

pending/: profiles waiting for validation and onboarding.

accepted/: profiles archived after successful onboarding.

rejected/: invalid profiles and validation error reports.

Do not place passwords, private keys, API tokens, or OpenSearch credentials inhospital profile YAML files.